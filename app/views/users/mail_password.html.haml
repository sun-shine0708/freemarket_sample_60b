- breadcrumb :"users/mail_password"

.header
  = render "layouts/header"

= render "layouts/nav"
.mail-password
  .mail-password-container__side-bar
    = render 'side-bar'

    .mail-password-content
      .mail-password-head
        メール/パスワード
      = form_for @user, url: user_path, method: :patch do |f|
        .mail-password-information
          .mail-password-box
            .mail
              .mp-label 
                メールアドレス
              = f.email_field :email, value: @user.email, placeholder: 'PC・携帯どちらでも可', class:'mp-form'
              .mp-text
                メールアドレスを変更すると確認メールが送信されます。メール内のURLをクリックすると変更完了です。
            .password
              .mp-label
                現在のパスワード
              = f.password_field :password, class:'mp-form'
              .mp-label
                新しいパスワード
              = f.password_field :password, placeholder: '7文字以上', class:'mp-form'
              .mp-label
                新しいパスワード確認
              = f.password_field :password_confirmation, placeholder: '7文字以上', class:'mp-form'
              .mp-text
                パスワードを設定したい場合は上記をすべて入力してください。（設定しない場合は空欄）
            = f.submit "変更する", class: 'btn mail-password-btn'

.app-banner
  = render "layouts/banner"

.footer
  = render "layouts/footer"

.display-btn
  = render "layouts/display_btn"
